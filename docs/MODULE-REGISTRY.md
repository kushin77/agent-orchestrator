# Module registry — the measured inventory, and the pointer to the contract

Status: **published and gated** (2026-09-14). Lane: issue #445 (parent #422);
control-plane evidence for the surface added by issue #591.
The machine-readable register this document describes is generated by
[`../governance/modules/`](../governance/modules/README.md); the gate that
enforces it is [`../scripts/check-module-registry.sh`](../scripts/check-module-registry.sh).

**The contract for this surface lives in the package README:
[`../governance/modules/README.md`](../governance/modules/README.md).** That
file is the single authority — the four surfaces and their precedence, the three
states (never two) plus the refusal that is not a state, determinism and
mutation-proofing, the CLI and its tri-state exit-code contract, the three
declared artifacts that judge a build (`controls.yaml`,
`module-registry.schema.json`, `audit.py`), the gate, and the NG4 boundary.
This page does not restate any of it: issue #1206 removed the near-verbatim
duplication that had grown between the two files, so what remains here is the
pointer above plus the material that is *measured* rather than *contractual*.

- The register itself: [`../governance/modules/`](../governance/modules/README.md)
  (`registry.py`; `cli.py build|verify|membership`).
- The gate that enforces it:
  [`../scripts/check-module-registry.sh`](../scripts/check-module-registry.sh).

## The refusal vocabulary (a refusal is not a state)

The package README owns the state model. These are the names it never folds into
the module list:

- `not-a-module` — the membership answer for any name the hub catalog does not
  carry: reported **by name**, counted separately, never a state
  (`kushin77/CMR#952`: portfolio membership is not module membership).
- `VENDOR-SOURCE-IN-TREE` — a hub module's `module.json` copied into this tree,
  or a submodule vendoring a module directly.
- `VENDOR-IN-TREE-PACKAGE` — a module's declared distribution package present
  in-tree (`codeidx/`, `node_modules/@kushin77/saas-rbac`).
- `VENDOR-EXTRA-SUBMODULE` — any vendored submodule path other than `vendor/CMR`.
- `VENDOR-PATH-OUTSIDE-HUB` — a registry reference that resolves outside the hub.

## The inventory this page records (measured 2026-09-14)

Measured in this lane's worktree over the pinned hub submodule (`vendor/CMR` at
the recorded gitlink) — reproduced from the authority, not from prose:

| Set | Members |
|---|---|
| **Registered mandatory (3)** | `code-indexing`, `diagrams`, `shared-frontend` |
| **Target-pending (3)** | `pmo`, `shared-governance`, `shared-services` |
| **Catalog modules, not mandatory (3)** | `erp-crm`, `googleworkspace`, `saas-rbac` |
| **Not a module (6)** | `deepseek`, `gcp-gatekeeper`, `hermes-agents`, `monitoring-stack`, `ollama`, `paperclip` |

The last six are *refusals*, not members: four come from this repo's admission
register (`deepseek`, `hermes-agents`, `ollama`, `paperclip`) and two from the hub
watch list (`monitoring-stack`, `gcp-gatekeeper`). None of them is carried by the
hub catalog at this pin.

### Where the measured hub disagrees with issue #445's table

The issue's table is a **claim**; the submodule is the authority. Two rows do not
survive contact with it, and this page records the measured truth rather than
papering the difference over:

| Issue #445's table | Measured |
|---|---|
| "Catalog modules, not mandatory (**8**)": `paperclip`, `hermes-agents`, `ollama`, `erp-crm`, `googleworkspace`, `saas-rbac`, plus the two pending once registered | **3** catalog modules are not mandatory. `paperclip`, `hermes-agents` and `ollama` are **not in the hub catalog at all** at this pin — they are not `catalog-module-not-mandatory`, they are `not-a-module`, and they are four with `deepseek`. Counting them as catalog modules was the CMR#952 drift itself. |
| the target set is `pmo`, `shared-governance`, `shared-services` | confirmed, and each names its blocking hub issue; the hub's own `docs/MANDATORY-MODULES.md` target-set table is the source the declarations cite |
| becoming-a-module `monitoring-stack` (`CMR#765`), open admission `gcp-gatekeeper` (`CMR#734`) | confirmed as **not** modules — both resolve to `not-a-module` with the hub issue named, never to `target-pending` |

Nothing in the disagreement is resolved by editing the hub (NG4): a change the hub
needs is a direction issue on the hub's board.

## The standards pin (issue #949, parent #878)

Separate from module *membership* is the spoke-side **standards pin**:
[`../cmr-pin.yaml`](../cmr-pin.yaml) at the repo root, prescribed by
`vendor/CMR/templates/module/cmr-pin.yaml` (EPIC-26/M23, GH #453, schema
`cmr.standards-pin/v1` in `vendor/CMR/sync/cmr-pin.schema.json`). It records
which CMR standards bundle (`bundle_ref`, the hub commit; `manifest_sha` and
`bundle_digest`, computed over `controller/standards-manifest.txt` at that
ref) this repo last synced — not registry membership, and admission for this
repo's own submodule claims in `module.json` stays `requested`, unchanged
by this pin.

Gated by [`../scripts/check-cmr-pin.sh`](../scripts/check-cmr-pin.sh) (wired
into `scripts/verify.sh` as `cmr-pin`): validates the pin against the hub's own
schema via `vendor/CMR/sync/validate-cmr-pin.py`, and refuses **drift** —
`bundle_ref` must equal the live `vendor/CMR` submodule HEAD — proved by a
provoked negative control (`CMR-PIN-DRIFT`, in a scratch copy, never the real
pin).
