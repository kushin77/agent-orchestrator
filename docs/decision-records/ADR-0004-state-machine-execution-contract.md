# ADR-0004: State-machine execution contract

**STATUS: reserved** for issues #21–#25 (phase 3 — State-machine execution).
Not a decision.

This number is held for the pillar-contract ADR that phase 3 will write: durable
orchestration engine, task lifecycle state machine + job queue, deterministic
agent-loop runtime, multi-agent orchestration, scoped agent memory. Standards to
adopt from `kushin77/shared-governance`
`GLOBAL_STANDARDS/schemas/agent-task.schema.json` and `brain-*.schema.json`.
When the phase-3 contract decision lands, replace this file with a full ADR from
[`template.md`](template.md) and update the [README index](README.md).
