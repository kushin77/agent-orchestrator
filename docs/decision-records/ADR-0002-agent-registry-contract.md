# ADR-0002: Agent Registry & Profiling contract

**STATUS: reserved** for issues #9–#14 (phase 1 — Agent Registry &
Profiling). Not a decision.

This number is held for the pillar-contract ADR that phase 1 will write:
declarative agent/persona/org schemas, agent identity + registry, SME persona
library, org RBAC/role model, prompt/instruction library, tenant onboarding.
Standards to adopt from `kushin77/shared-governance`
`GLOBAL_STANDARDS/schemas/agent-identity-jwt.schema.json` and the
agent-orchestration model. When the phase-1 contract decision lands, replace
this file with a full ADR from [`template.md`](template.md) and update the
[README index](README.md).
