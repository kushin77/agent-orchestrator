# ADR-0003: Model Gateways contract

**STATUS: reserved** for issues #15–#20 (phase 2 — Model Gateways). Not a
decision.

This number is held for the pillar-contract ADR that phase 2 will write:
multi-provider client adapters, gateway proxy routing, FinOps model chooser,
model health/auto-degradation/fallback, semantic cache + token budget + rate
limits, tenant-scoped MCP tool gateway. Standards to adopt from
`kushin77/shared-governance` `GLOBAL_STANDARDS/` (egress policy, rate-limit and
telemetry-budget schemas). When the phase-2 contract decision lands, replace
this file with a full ADR from [`template.md`](template.md) and update the
[README index](README.md).
