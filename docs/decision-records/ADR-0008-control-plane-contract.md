# ADR-0008: Control plane / portal contract

**STATUS: reserved** for issues #38–#42 (phase 7 — Control plane / portal). Not
a decision.

This number is held for the pillar-contract ADR that phase 7 will write:
control-plane REST API + event/outbox bus, admin/tenant portal (design tokens +
SSO), agent-pack registry + catalog, consumer SDK + born-compliant templates,
model-agnostic instruction layer + per-tool mirrors. Standards to adopt from
`kushin77/shared-governance` `GLOBAL_STANDARDS/` (agent-orchestration-model,
service-catalog schema). When the phase-7 contract decision lands, replace this
file with a full ADR from [`template.md`](template.md) and update the
[README index](README.md).
