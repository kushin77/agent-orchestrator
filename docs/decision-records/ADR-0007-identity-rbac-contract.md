# ADR-0007: Tenant identity / RBAC contract

**STATUS: reserved** for issues #35–#37 (phase 6 — Tenant identity / RBAC). Not
a decision.

This number is held for the pillar-contract ADR that phase 6 will write: tenant
identity + SSO (tenant-to-IdP mapping, SAML/OIDC), RBAC + entitlement tiering,
public API + proxy allowlist boundary (authn is never authz). Standards to adopt
from `kushin77/shared-governance` `GLOBAL_STANDARDS/` (agent-oidc-config and
agent-identity-jwt schemas). When the phase-6 contract decision lands, replace
this file with a full ADR from [`template.md`](template.md) and update the
[README index](README.md).
