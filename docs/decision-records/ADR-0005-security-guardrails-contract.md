# ADR-0005: Security & guardrails contract

**STATUS: reserved** for issues #26–#30 (phase 4 — Security & guardrails). Not
a decision.

This number is held for the pillar-contract ADR that phase 4 will write:
policy-as-code + gate engine, DLP + prompt-injection defense + egress guard,
guard honesty (tri-state + negative controls), verification/QA gate stack,
tenant-isolation integrity. Standards to adopt from `kushin77/shared-governance`
`GLOBAL_STANDARDS/` (approval-gate, external-llm-egress-policy, agent-action and
scrub-rules schemas). When the phase-4 contract decision lands, replace this
file with a full ADR from [`template.md`](template.md) and update the
[README index](README.md).
