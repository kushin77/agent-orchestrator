# DELIBERATELY BROKEN fixture: defaultModelTier 'ULTRA' is not a known tier
# (valid: LOW, MED, HIGH, MAX). Must be rejected.
id: coder
version: 1.0.0
owner: platform/execution
systemPromptRef: coder/primary@v1
toolAllowlist:
  - gh_issue
constraintSet:
  - issue-first
capabilitySet:
  - code-author
defaultModelTier: ULTRA
memoryScope:
  - session
guardrailPolicyRef: worker-bundle
