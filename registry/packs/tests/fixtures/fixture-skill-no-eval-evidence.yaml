# tests/fixtures/fixture-skill-no-eval-evidence.yaml — a skill shipped WITHOUT
# eval evidence (issue #640).
#
# The publish gate (registry/packs/skills.py) refuses a skill with no eval
# evidence, and validate.py's code-native contents check refuses a pack whose
# `contents.skill` entry is unevaluated. This fixture is the pack that must be
# REJECTED: drop `evalEvidence` and delete the entry's skill metadata and the
# pack is structurally wrong in exactly the way the gate exists to catch.
#
# Registered in the self-test sweep (tests/fixtures/* is required to FAIL).
schema: agent-pack/v1
id: sleeper-skills
version: 1.0.0
name: Sleeper skills
category: coding
upstream: "https://github.com/kushin77/agent-orchestrator"
publisher: platform/execution
lifecycle: live
description: >-
  A pack that bundles a skill with no eval evidence — must be refused.
contents:
  skill:
    - ref: codemod@1.0.0
      skillId: codemod
      skillVersion: 1.0.0
      skillCategory: code-authoring
      skillLifecycle: published
      data: c2tpbGw6IGNvZGVtb2QK
      sha256: 0e6d4a5d4d4a8b9e5f3f8f1e2a7c6b5d4e3f2a1b0c9d8e7f6a5b4c3d2e1f0a9b
attestation:
  kid: ao-pack-publisher-v1
  alg: PS256
  signedAt: "2026-09-08T00:00:00Z"
  signature: QUFBQUFBQUFBQUFBQUFBQQ==
